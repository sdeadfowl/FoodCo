@{
    if (User.Identity.IsAuthenticated)
    {
        Layout = "~/Views/Shared/_LoggedInLayout.cshtml";  // Layout for logged-in users
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";  // Layout for anonymous users
    }
}
<h2>Erreur 400 - Requête incorrecte</h2>
<p>La requête envoyée au serveur est incorrecte. Veuillez vérifier les informations et réessayer.</p>
